div #[b= entry.title]
div Créé par #[a(href="/member/"+entry.createdBy.key): b #{entry.createdBy.username}]
if entry.sc
	h5 Événement Star Citizen
div 📅 du #[b #{dateformat(entry.startDate, "d mmm yyyy à HH:MM")}] au #[b #{dateformat(entry.endDate, "d mmm yyyy à HH:MM")}]

hr
div!= content
	
if entry.public
	div(style="float: left;")
		if entry.public
			span.label.label-success Public

if status !== undefined
	div(style="float: right;")
		if status === "present"
			span.label.label-success Accepté
		else if status === "away"
			span.label.label-danger Refusé
		else if status === "maybe"
			span.label.label-warning Peut-être
		else
			span.label.label-primary Pas encore inscrit
